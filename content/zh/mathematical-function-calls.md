---
title: "数学函数调用"
parent: "表达式"
---


## 最大值

从指定的参数返回最大值。

### 输入参数

*   两个或多个值都是类型的日期和时间，或者是数字类型(整数/长、 浮点或十进制)。

### 产出

从指定的参数返回最大值。 如果参数是类型日期和时间，结果也将是类型日期和时间。 如果参数是数字的，结果将是最精确的类型。 例如，如果指定了整数/长数和小数点数两个参数，结果将是小数点数。

类型：整数/小数

```java
max(5, 1, 5, 6.7)
```

返回：

```java
6.7
```

类型“十进制”。

## 分钟

从指定参数返回最小值。

### Input

两个或多个值都是类型的日期和时间，或者是数字类型(整数/长、 浮点或十进制)。

### 产出

从指定参数返回最小值。 如果参数是类型日期和时间，结果也将是类型日期和时间。 如果参数是数字的，结果将是最精确的类型。 例如，如果指定了整数/长数和小数点数两个参数，结果将是小数点数。

类型：整数/小数

```java
最小(5, 1, 5, 6.7)
```

返回：

```java
1
```

类型“十进制”

## 圆形

将一个数调到一定的精度。

### Input

*   数字 类型：整数/Long、浮动或小数

*   精度(可选) 类型：整数/经度

### 产出

在 **设置**中，您的 **圆号** 选项可以设置：
*   在 **距离0** 选项(也叫做“商业环绕”) 一半的地方，+2.5 变成+3 ，-1.5 变成-2
*   **到最近的偶数** 选项的一半(也称为“银行家环形”)，是  [IEEE 754](http://en.wikipedia.org/wiki/IEEE_floating_point "IEEE floating point") 计算函数和操作员使用的默认四舍五入模式； 例如，+23. 变成+24，变成+24.5；并变成-22，变成-21.5。

第二个可选参数决定四舍五入的精度。 默认值为0。 结果将是最准确的类型。 如果精度为 0，结果将为整数/长型。 对于所有其他精确值来说，结果将是十进制型。

类型：整数/小数

```java
圆形(3.5)
```

返回：

```java
4
```

类型“整数/中”

和

```java
圆形(88.725,2)
```

返回：

```java
88.72
```

十进制类型

## 随机的

生成随机数字 >= 0.0 和 < 1.0

### 产出

0.0 和 1.0 之间的随机数字 类型：十进制数

```java
随机()
```

## 地板

向下回转到整数(返回小于或等于输入的最大整数)。

### Input

*   数字 类型：整数/Long、浮动或小数

### 产出

输入值四舍五入到最近的整数。

类型：Integer/Long

```java
下限(3.9)
```

返回：

```java
3
```

和

```java
floor(-1.2)
```

返回：

```java
-2
```

## 小牛座

响铃到整数(返回大于或等于输入的最小整数)。

### Input

*   数字 类型：整数/Long、浮动或小数

### 产出

输入值四舍五入到最近的整数。

类型：Integer/Long

```java
最高限额（3.2）
```

返回：

```java
4
```

和

```java
天花板(1.9)
```

返回：

```java
-1
```

## pow

计算一个数值对特定力量的指数值。

### Input

*   数字 类型：整数/Long、浮动或小数
*   电源 类型：整数/Long、浮动或小数

### 产出

电源的数字，如在n^p中。 结果将是必要的最准确的类型。

类型：整数/小数

```java
pow(2, 3)
```

返回：

```java
8
```

类型“整数/中”

和

```java
pow(2.5, 3)
```

返回：

```java
15.625
```

类型“十进制”

使用小数指数计算“粉末”可能不准确，因为标准的 Java 库不支持这些计算。 在自定义的 Java 动作中使用一个专业知识库，如果这种情况需要高度精准的话。

## 位数

计算数字的绝对值 (不为负值)。

### Input

*   数字 类型：整数/Long、浮动或小数

### 产出

输入的绝对值，从来都不是负值。 相应地占用正方形，然后占用正方根。

类型：整数/小数

```java
缺席(-5)
```

和

```java
缺席（5）
```

两个返回：

```java
5
```
## 浮点型变量等于

将两个数字压缩为十进制点p, 等于精度.

{{% alert type="warning" %}}

此函数与浮动类型一起被废弃。 使用高精度小数点后缀。

{{% /报警 %}}

### Input

*   数字 类型：整数/Long 或 Float
*   另一个数字 类型：整数/Long 或 Float
*   精度 类型：整数/经度

### 产出

显示指定精确度的两个数字是否等于的值。

Type: Boolean

```java
浮点等价(0.51, 0.50, 1)
```

返回：

```java
true
```

和

```java
浮点等价(0.51、0.50, 2)
```

返回：

```java
false
```

## 货币等于

查看浮点等值。

{{% alert type="warning" %}}

此函数与浮动类型一起被废弃。 使用高精度小数点后缀。

{{% /报警 %}}
