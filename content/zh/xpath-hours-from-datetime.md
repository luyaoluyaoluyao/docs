---
title: "XPath 小时从日期时间开始"
parent: "xpate-constraint-function"
---

## 1 概览

`小时-dateTime()` 函数从日期时间属性中提取小时数，以便它可以用于比较一个值。

## 2 个示例

此查询返回 `日期属性` 中小时数量为8小时的所有日志(例如，"2011-12-30 08:00:00")：

```java
//Logging.log[小时-日期时间(DateAttribute) = 8]
```
